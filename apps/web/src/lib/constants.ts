// API and pagination constants

/** Default limit for Kanban board - load all enquiries for drag-drop */
export const KANBAN_DEFAULT_LIMIT = 200;

/** Default limit for dropdown selects (contacts, etc.) */
export const DROPDOWN_DEFAULT_LIMIT = 200;

/** Default limit for list views */
export const LIST_DEFAULT_LIMIT = 50;

/** Maximum activities to show in timeline */
export const ACTIVITY_TIMELINE_LIMIT = 50;
